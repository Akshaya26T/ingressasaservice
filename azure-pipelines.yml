trigger:
  branches:
    include:
      - main
      - develop
      - feature/*

pr:
  branches:
    include:
      - "*"

variables:
  composeFile: docker-compose.yml
  composeProject: ingress-ci
  baseUrl: http://127.0.0.1:3000
  deployEnabled: 'true'
  deployPool: mac-agent-pool
  deployAgentName: mac-build-agent

stages:
  - stage: CI
    displayName: Build and validate stack
    jobs:
      - job: BuildAndTest
        displayName: Build images and run smoke tests
        pool:
          vmImage: ubuntu-latest
        steps:
          - checkout: self

          - script: |
              docker --version
              docker compose version
            displayName: Show Docker versions

          - script: |
              chmod +x check_canary_split.sh
              chmod +x scripts/ci-smoke-test.sh
            displayName: Make scripts executable

          - script: docker compose -f $(composeFile) config
            displayName: Validate compose file

          - script: |
              export COMPOSE_PROJECT_NAME=$(composeProject)
              docker compose -f $(composeFile) build app1 app1-canary app2 app3
            displayName: Build service images

          - script: |
              export COMPOSE_PROJECT_NAME=$(composeProject)
              docker compose -f $(composeFile) up -d traefik app1 app1-canary app2 app3
              docker compose -f $(composeFile) ps
            displayName: Start stack

          - script: |
              export COMPOSE_PROJECT_NAME=$(composeProject)
              port=$(docker compose -f $(composeFile) port traefik 80 | tail -1 | sed -E 's/.*:([0-9]+)$/\1/')
              if [ -z "$port" ]; then
                echo "Unable to determine published Traefik port"
                exit 1
              fi
              echo "Resolved Traefik port: $port"
              echo "##vso[task.setvariable variable=runtimeBaseUrl]http://127.0.0.1:$port"
            displayName: Resolve runtime base URL

          - script: scripts/ci-smoke-test.sh $(runtimeBaseUrl)
            displayName: Run smoke tests

          - script: |
              export COMPOSE_PROJECT_NAME=$(composeProject)
              docker compose -f $(composeFile) logs --no-color > compose.log
            displayName: Capture compose logs
            condition: always()

          - publish: compose.log
            artifact: compose-logs
            displayName: Publish logs artifact
            condition: always()

          - publish: $(Build.SourcesDirectory)
            artifact: source-drop
            displayName: Publish source artifact

          - script: |
              export COMPOSE_PROJECT_NAME=$(composeProject)
              docker compose -f $(composeFile) down -v --remove-orphans
            displayName: Tear down stack
            condition: always()

  - stage: CD
    displayName: Deploy via self-hosted agent
    dependsOn: CI
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'), eq(variables['deployEnabled'], 'true'))
    jobs:
      - job: DeployComposeStack
        displayName: Deploy compose stack on mac-build-agent
        pool:
          name: $(deployPool)
          demands:
            - Agent.Name -equals $(deployAgentName)
        steps:
          - checkout: none

          - download: current
            artifact: source-drop
            displayName: Download source artifact

          - script: |
              chmod +x check_canary_split.sh
              chmod +x scripts/ci-smoke-test.sh
            displayName: Make scripts executable
            workingDirectory: $(Pipeline.Workspace)/source-drop

          - script: |
              docker --version
              docker compose version
            displayName: Verify Docker on deployment agent

          - script: |
              export COMPOSE_PROJECT_NAME=ingressasaservice
              docker compose -f $(composeFile) pull || true
              docker compose -f $(composeFile) up -d --build
              docker compose -f $(composeFile) ps
            displayName: Deploy stack
            workingDirectory: $(Pipeline.Workspace)/source-drop

          - script: |
              export COMPOSE_PROJECT_NAME=ingressasaservice
              port=$(docker compose -f $(composeFile) port traefik 80 | tail -1 | sed -E 's/.*:([0-9]+)$/\1/')
              if [ -z "$port" ]; then
                echo "Unable to determine published Traefik port"
                docker compose -f $(composeFile) ps
                exit 1
              fi
              echo "Resolved Traefik port: $port"
              echo "##vso[task.setvariable variable=runtimeBaseUrl]http://127.0.0.1:$port"
            displayName: Resolve deployed runtime base URL
            workingDirectory: $(Pipeline.Workspace)/source-drop

          - script: scripts/ci-smoke-test.sh $(runtimeBaseUrl)
            displayName: Run post-deploy smoke tests
            workingDirectory: $(Pipeline.Workspace)/source-drop
